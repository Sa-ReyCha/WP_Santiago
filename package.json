{
  "name": "wp-santiago",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/styles.css --minify && cp src/index.html dist/index.html && mkdir -p dist/projects && cp src/projects/*.html dist/projects/ && mkdir -p dist/public && cp -R public/. dist/public/",
    "dev": "tailwindcss -i ./src/input.css -o ./src/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
